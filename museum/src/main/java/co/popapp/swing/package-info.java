/**
 * <p>This package contains Swing controls and helpers. The components are not part of the
 * application GUI, but reusable components not tied to this application.</p>
 *
 * <p>This package should not rely on any other and eventually if the size increased can be
 * extracted to other project, module or JAR (using classifiers).</p>
 *
 * <p>The application user interface (screens and dialogs) are inside {@link co.popapp.gui}
 */
package co.popapp.swing;
